import SwiftUI

extension Color {
    static let ratioPrimary = Color("RatioPrimary")
    static let ratioBackground = Color("RatioBackground")
    static let ratioSurface = Color("RatioSurface")
    
    // Fallback if assets are not set yet
    static let fallbackPrimary = Color.blue
}
